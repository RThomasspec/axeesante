{% extends 'base.html.twig' %}

{% block body %}
	{{form_start(formulairepatient)}}
	<div class="form-group">
		{{ form_label(formulairepatient.nom, 'Nom :', {'label_attr': {'class': 'form-label mt-4'}}) }}
		{{ form_widget(formulairepatient.nom, {'attr': {'class': 'form-control'}}) }}
		{{ form_errors(formulairepatient.nom) }}
	</div>
	<div class="form-group">
		{{ form_label(formulairepatient.prenom, 'Prénom :', {'label_attr': {'class': 'form-label mt-4'}}) }}
		{{ form_widget(formulairepatient.prenom, {'attr': {'class': 'form-control'}}) }}
		{{ form_errors(formulairepatient.prenom) }}
	</div>

	<div class="form-group">
		{{ form_label(formulairepatient.adresse, 'Adresse :', {'label_attr': {'class': 'form-label mt-4'}}) }}
		{{ form_widget(formulairepatient.adresse, {'attr': {'class': 'form-control'}}) }}
		{{ form_errors(formulairepatient.adresse) }}
	</div>


	<div class="form-group">
		{{ form_label(formulairepatient.datedenaissance, 'Date de naissance :', {'label_attr': {'class': 'form-label mt-4'}}) }}
		{{ form_widget(formulairepatient.datedenaissance, {'attr': {'class': 'form-control'}}) }}
		{{ form_errors(formulairepatient.datedenaissance) }}
	</div>

	<button type="submit">
		envoyer
	</button>
{% endblock %}
